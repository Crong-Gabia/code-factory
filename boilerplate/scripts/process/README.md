# Document-driven Process

This directory contains scripts that demonstrate a document-driven process.

- `ROADMAP.md` is treated as the process source of truth.
- Changing a step to `(IN_PROGRESS)` triggers the process runner.
