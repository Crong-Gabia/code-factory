# dev-factory

Project generator that creates a local Docker-based development factory boilerplate.

## Build

```bash
npm install
npm run build
```

## Generate a project (interactive)

```bash
npm run factory:init
```

Generated projects are created under:

- `dev-factory/output/<project-name>`
